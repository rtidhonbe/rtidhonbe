# Server
PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string

# Anonymity — set to socks5://127.0.0.1:9050 when running behind Tor
PROXY_URL=

# Rate limiting
MAX_SENDS_PER_HOUR=50
DELAY_MIN_MS=4000
DELAY_MAX_MS=9000
